apiVersion: v1
kind: Service
metadata:
  name: {{ include "company-portal.fullname" . }}
  labels:
    {{- include "company-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "company-portal.selectorLabels" . | nindent 4 }}
