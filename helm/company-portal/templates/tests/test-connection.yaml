apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "company-portal.fullname" . }}-test-connection"
  labels:
    {{- include "company-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "company-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
